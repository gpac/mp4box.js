declare namespace BoxParser {
    class cttsBox {
        unpack(samples: any[]): void;
    }

    class sttsBox {
        unpack(samples: any[]): void;
    }

    class stcoBox {
        unpack(samples: any[]): void;
    }

    class stscBox {
        unpack(samples: any[]): void;
    }

    class stszBox {
        unpack(samples: any[]): void;
    }
}
